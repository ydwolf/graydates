GD.addTopic({
  id: "dsa",
  name: "Data Structures & Big-O",
  sigil: "[ ]",
  blurb: "What costs what, and why it matters",
  questions: [
    { tag: "Arrays",
      q: "What is the time complexity of reading <code>arr[5000]</code> in an array?",
      choices: ["O(1)", "O(log n)", "O(n)", "O(n log n)"],
      answer: 0,
      why: "Array elements sit in contiguous memory, so the address is arithmetic: base + index × element size. The index makes no difference to the cost.",
      wrong: { 1: "O(log n) is repeated halving, as in binary search — no search is happening here.",
               2: "O(n) is what a linked list costs, because it has to walk from the head.",
               3: "O(n log n) is the cost of a good comparison sort." } },

    { tag: "Hash tables",
      q: "What is the <em>average</em> lookup time in a hash map?",
      choices: ["O(1)", "O(log n)", "O(n)", "O(n²)"],
      answer: 0,
      why: "Hashing the key jumps straight to a bucket, so lookup does not depend on the number of entries. Worst case degrades to O(n) if every key collides into one bucket.",
      wrong: { 1: "O(log n) is a balanced tree, which a hash map beats on average — at the cost of losing sorted order.",
               2: "O(n) is the pathological worst case, not the average.",
               3: "O(n²) does not describe any single lookup." } },

    { tag: "Stacks & queues",
      q: "A stack is LIFO. What does that mean?",
      choices: ["The first item pushed is the first popped", "The last item pushed is the first popped", "Items pop in sorted order", "Items pop at random"],
      answer: 1,
      why: "Last in, first out — like a stack of plates. It is exactly how the call stack works, which is why recursion returns in reverse order of the calls.",
      wrong: { 0: "That is FIFO, which describes a queue.",
               2: "Sorted removal is a priority queue or heap.",
               3: "Stacks are strictly ordered; nothing is random." } },

    { tag: "Linked lists",
      q: "Compared with an array, a singly linked list gives you —",
      choices: ["O(1) access by index", "O(1) insertion at the head, but O(n) access by index", "O(1) for everything", "Better cache locality"],
      answer: 1,
      why: "Inserting at the head is just two pointer writes. But there is no index arithmetic — reaching element <em>k</em> means following <em>k</em> links one at a time.",
      wrong: { 0: "Indexed access is precisely what a linked list gives up.",
               2: "No structure is O(1) for everything — every one of them trades something away.",
               3: "The opposite. Nodes are scattered across the heap, so a linked list is far less cache-friendly than a packed array." } },

    { tag: "Searching",
      q: "Binary search runs in O(log n), but only if —",
      choices: ["The collection is sorted", "The collection is a linked list", "All values are unique", "The collection fits in memory"],
      answer: 0,
      why: "Each step throws away half the remaining range based on a comparison with the middle. That only works if order tells you which half to keep.",
      wrong: { 1: "Binary search on a linked list is pointless — reaching the middle already costs O(n).",
               2: "Duplicates are fine. You still find <em>a</em> match; finding the first takes a small tweak.",
               3: "Binary search works on disk and across networks too. Sorted order is the requirement." } },

    { tag: "Sorting",
      q: "What is merge sort's time complexity, and what does it cost in space?",
      choices: ["O(n log n) time, O(n) extra space", "O(n²) time, O(1) space", "O(n) time, O(n) space", "O(log n) time, O(1) space"],
      answer: 0,
      why: "It halves the input log n times and does O(n) work merging at each level. The merge step needs a scratch buffer, so it is not in-place — but it is stable and its worst case is still O(n log n).",
      wrong: { 1: "O(n²) with O(1) space describes bubble or insertion sort.",
               2: "No comparison sort can beat O(n log n). O(n) sorting needs assumptions about the data, as counting sort makes.",
               3: "You cannot sort n items in less time than it takes to look at them all." } },

    { tag: "Sorting",
      q: "Quicksort is usually the fastest in practice, but its worst case is —",
      choices: ["O(n log n)", "O(n²), when pivots are chosen badly", "O(n)", "O(2ⁿ)"],
      answer: 1,
      why: "If every pivot is the smallest or largest remaining element — already-sorted input with a naive pivot choice — the partitions never balance and it degrades to O(n²). Random or median-of-three pivots make that vanishingly unlikely.",
      wrong: { 0: "That is its average case, and merge sort's guaranteed case.",
               2: "Comparison sorting cannot be linear.",
               3: "Exponential time belongs to brute-force subset problems, not sorting." } },

    { tag: "Heaps",
      q: "In a min-heap, what do peek and insert cost?",
      choices: ["Peek O(1), insert O(log n)", "Peek O(n), insert O(1)", "Both O(1)", "Both O(n)"],
      answer: 0,
      why: "The minimum is always at the root, so peeking is free. Inserting appends at the bottom and sifts up at most the height of the tree — O(log n).",
      wrong: { 1: "Finding the minimum never requires a scan; keeping it at the root is the point of a heap.",
               2: "Insertion has to restore the heap property, which can bubble all the way to the root.",
               3: "O(n) for both would make a heap no better than an unsorted array." } },

    { tag: "Big-O",
      q: "What is the complexity of this?<pre>for i in range(n):\n    for j in range(n):\n        total += grid[i][j]</pre>",
      choices: ["O(n)", "O(n log n)", "O(n²)", "O(2ⁿ)"],
      answer: 2,
      why: "The inner loop runs n times for each of the n outer iterations — n × n operations in total.",
      wrong: { 0: "O(n) would be a single loop. There are two, and they are nested.",
               1: "O(n log n) appears when one dimension halves each round, which is not happening here.",
               3: "Exponential growth would require branching into two recursive calls per step." } },

    { tag: "Big-O",
      q: "An algorithm takes <code>3n + 200</code> steps. What is its Big-O?",
      choices: ["O(3n + 200)", "O(n)", "O(200)", "O(3n)"],
      answer: 1,
      why: "Big-O describes growth, so constant factors and lower-order terms drop away. As n grows large, tripling and the flat 200 stop mattering — it is linear.",
      wrong: { 0: "Big-O is never written with the constants left in.",
               2: "O(200) would mean constant time, but the cost genuinely grows with n.",
               3: "The coefficient 3 is dropped for the same reason the 200 is." } },

    { tag: "Graphs",
      q: "BFS and DFS differ mainly in what?",
      choices: ["BFS uses a queue and explores level by level; DFS uses a stack or recursion and plunges down one path", "BFS is always faster", "DFS only works on trees", "BFS cannot find a path"],
      answer: 0,
      why: "The data structure is the whole difference. A queue yields the level-order sweep that finds shortest paths in unweighted graphs; a stack yields deep-first exploration, which suits cycle detection and topological sorting.",
      wrong: { 1: "Both visit every reachable node in O(V + E). Which finds the target sooner depends entirely on where it is.",
               2: "DFS works on any graph, as long as you track visited nodes to avoid looping.",
               3: "BFS finds paths — and in an unweighted graph, it finds the shortest one." } },

    { tag: "Hash tables",
      q: "Two keys hash to the same bucket. In separate chaining, what happens?",
      choices: ["The second insert fails", "The first value is overwritten", "The bucket holds a list, and both entries live there", "The table is rebuilt from scratch"],
      answer: 2,
      why: "Each bucket holds a small collection — often a linked list. Lookups find the bucket, then scan it comparing keys. Long chains are what turn O(1) into O(n).",
      wrong: { 0: "Collisions are expected and routine, not an error condition.",
               1: "Only an equal <em>key</em> overwrites. Different keys that merely collide both survive.",
               3: "Resizing happens when the load factor gets high, not on each collision." } },

    { tag: "Trees",
      q: "An in-order traversal of a binary search tree visits the values —",
      choices: ["In sorted order", "In random order", "Level by level", "In reverse insertion order"],
      answer: 0,
      why: "In-order means left subtree, node, right subtree. Since a BST keeps everything smaller on the left, that walk produces ascending order — which is a neat way to sort while validating the tree.",
      wrong: { 1: "The structure is fully deterministic; nothing about it is random.",
               2: "Level by level is a breadth-first traversal, which uses a queue.",
               3: "Insertion order is not recorded anywhere in the tree." } },

    { tag: "Trees",
      q: "Search in a <em>balanced</em> BST is O(log n). What is it in an unbalanced one?",
      choices: ["Still O(log n)", "O(n) in the worst case — the tree degenerates into a linked list", "O(1)", "O(n²)"],
      answer: 1,
      why: "Insert already-sorted data into a naive BST and every node gets one child. The height becomes n, and search walks the whole chain. Self-balancing trees (AVL, red-black) exist precisely to prevent this.",
      wrong: { 0: "O(log n) depends on the height staying logarithmic, which is exactly what balance guarantees.",
               2: "No tree search is constant time; you always descend at least the height.",
               3: "A single search never revisits nodes, so it cannot be quadratic." } },

    { tag: "Dynamic arrays",
      q: "Python lists and Java ArrayLists append in <em>amortized</em> O(1). What does that mean?",
      choices: ["Every append is exactly one operation", "Most appends are O(1); occasionally the array doubles and copies, averaging out to O(1)", "Appending is O(n) but feels fast", "It only holds for small lists"],
      answer: 1,
      why: "When capacity runs out the array allocates a larger block (typically double) and copies everything — an O(n) step. Because doubling makes those resizes exponentially rare, the cost spread across all appends is constant.",
      wrong: { 0: "The resizing appends really do cost O(n). Amortized analysis averages them in.",
               2: "The average really is O(1) — not merely a perception.",
               3: "The analysis holds at any size; that is the point of a growth factor." } },

    { tag: "Recursion",
      q: "A recursive function recurses n deep. What is its space complexity?",
      choices: ["O(1)", "O(log n)", "O(n), from the call stack", "O(n²)"],
      answer: 2,
      why: "Each pending call keeps a stack frame holding its parameters and return address. Depth n means n frames alive at once — which is what a stack-overflow crash is telling you.",
      wrong: { 0: "Only a tail call optimized into a loop achieves O(1), and Python does not do that.",
               1: "O(log n) would be a recursion that halves each time, like binary search.",
               3: "The stack holds one frame per level, not one per pair of levels." } },

    { tag: "Applications",
      q: "Given an array, find whether two numbers sum to a target. What does a hash set buy you?",
      choices: ["It drops O(n²) brute force to O(n)", "It sorts the array for free", "It removes the need for a loop", "Nothing — brute force is optimal"],
      answer: 0,
      why: "Walk the array once, and at each element check whether <code>target − x</code> has already been seen. One pass, O(1) lookups, so O(n) time for O(n) extra space.",
      wrong: { 1: "Hash sets have no order at all. That is what you give up for constant-time lookup.",
               2: "You still make one pass — the loop is what does the work.",
               3: "Brute force compares every pair at O(n²). Linear is a large improvement." } },

    { tag: "Graphs",
      q: "How much space does an adjacency list use for a graph with V vertices and E edges?",
      choices: ["O(V²)", "O(V + E)", "O(E²)", "O(1)"],
      answer: 1,
      why: "One entry per vertex plus one per edge. For sparse graphs — a road network, a social graph — that is dramatically less than the V² a full matrix would demand.",
      wrong: { 0: "O(V²) is the adjacency <em>matrix</em>, which wastes space when most pairs are unconnected but answers &quot;is there an edge?&quot; in O(1).",
               2: "Edges are stored once each, not compared pairwise.",
               3: "You have to store the graph somewhere; it cannot be constant." } },

    { tag: "Tries",
      q: "What is a trie especially good at?",
      choices: ["Sorting numbers", "Prefix queries — autocomplete, dictionary lookup", "Storing images", "Finding the median"],
      answer: 1,
      why: "A trie stores strings along paths of characters, so every node <em>is</em> a prefix. Finding all words starting with &quot;pyt&quot; means walking three edges and collecting the subtree — with lookup cost tied to word length, not dictionary size.",
      wrong: { 0: "Numbers are usually handled by comparison sorts or radix sort.",
               2: "Binary blobs have no meaningful shared prefixes to exploit.",
               3: "Medians come from heaps, quickselect or sorting." } },

    { tag: "Choosing",
      q: "You need to repeatedly pull out the smallest item while inserting new ones. What fits best?",
      choices: ["A sorted array", "A priority queue (heap)", "A hash set", "A stack"],
      answer: 1,
      why: "A heap gives O(1) peek and O(log n) for both insert and extract-min — the exact mix this workload asks for. It is the structure behind Dijkstra's algorithm and most task schedulers.",
      wrong: { 0: "Keeping an array sorted costs O(n) per insert, since everything after the insertion point shifts.",
               2: "A hash set has no notion of smallest. Finding the minimum would mean scanning all of it.",
               3: "A stack hands back the most recent item, which has nothing to do with the smallest." } }
  ]
});
