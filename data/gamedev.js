GD.addTopic({
  id: "gamedev",
  name: "Game Development",
  sigil: "▶",
  blurb: "Loops, delta time, collisions, engines",
  questions: [
    { tag: "Game loop",
      q: "What are the three phases of a classic game loop, in order?",
      choices: ["Render → input → update", "Input → update → render", "Update → input → render", "Load → render → save"],
      answer: 1,
      why: "Every frame reads input, advances the simulation by one step, then draws the resulting world. Drawing last means the player always sees the state that input just produced.",
      wrong: { 0: "Rendering first would draw the previous frame's world, adding a frame of input lag.",
               2: "Updating before reading input means this frame's key presses are ignored until the next one.",
               3: "Loading and saving happen occasionally, not sixty times per second." } },

    { tag: "Delta time",
      q: "Why multiply movement by delta time — <code>pos += speed * dt</code>?",
      choices: ["To make the game run faster", "So motion is the same speed regardless of frame rate", "To convert pixels into meters", "To smooth out the graphics"],
      answer: 1,
      why: "<code>dt</code> is the seconds elapsed since the previous frame. Scaling by it turns &quot;pixels per frame&quot; into &quot;pixels per second,&quot; so a 30 fps machine and a 144 fps machine move the player the same distance.",
      wrong: { 0: "It changes nothing about how fast the game runs — only about how far things move each frame.",
               2: "Unit conversion is a scale factor you pick yourself; <code>dt</code> is purely about time.",
               3: "Smoothing is interpolation or anti-aliasing. Delta time is about correctness of speed." } },

    { tag: "Performance",
      q: "At a locked 60 FPS, roughly how long does one frame have to do all its work?",
      choices: ["16.7 ms", "60 ms", "1 ms", "100 ms"],
      answer: 0,
      why: "One second divided by 60 frames is about 16.7 milliseconds. Everything — input, physics, AI, draw calls — has to fit in that budget or the frame is dropped.",
      wrong: { 1: "60 ms would be about 16 FPS, a visible stutter.",
               2: "1 ms would be 1000 FPS. Nice if you can get it, but not what 60 FPS demands.",
               3: "100 ms per frame is 10 FPS — a slideshow." } },

    { tag: "Collision",
      q: "Two axis-aligned rectangles overlap when —",
      choices: ["Their centers are within 10 pixels", "They overlap on <strong>both</strong> the x axis and the y axis", "They overlap on <strong>either</strong> the x axis or the y axis", "Their areas are equal"],
      answer: 1,
      why: "An AABB test is two 1-D range checks combined: if the x ranges overlap AND the y ranges overlap, the boxes intersect. Separation on either axis alone proves they miss.",
      wrong: { 0: "A fixed pixel distance is a circle test, and it ignores the size of each box.",
               2: "&quot;Either&quot; is far too loose — two boxes at opposite corners of the screen can share an x range and never touch.",
               3: "Equal area says nothing about position. Two identical boxes miles apart do not collide." } },

    { tag: "Vectors",
      q: "A player pressing right + up moves faster diagonally than straight. Why, and what fixes it?",
      choices: ["Rounding error; use floats", "The combined vector has length √2; normalize it before applying speed", "The frame rate doubles; cap it", "Two inputs queue up; ignore one"],
      answer: 1,
      why: "Adding (1,0) and (0,1) gives a vector of length about 1.41, not 1. Normalizing the input direction to unit length before multiplying by speed makes every direction equally fast.",
      wrong: { 0: "The value is exactly √2 — an honest geometric result, not a floating-point artifact.",
               2: "The frame rate is unaffected by which keys are held.",
               3: "Ignoring an input would break diagonal movement entirely instead of correcting its speed." } },

    { tag: "Rendering",
      q: "Why pack many sprites into one sprite sheet (texture atlas)?",
      choices: ["It compresses the art losslessly", "It cuts texture swaps, letting many sprites draw in one batched draw call", "It is required by all engines", "It makes the sprites render larger"],
      answer: 1,
      why: "Switching the bound texture breaks batching. If every sprite in a scene lives on one atlas, the GPU can draw them together, turning hundreds of draw calls into a handful.",
      wrong: { 0: "Compression is a property of the image format, not of how the frames are arranged.",
               2: "You can absolutely load individual image files. Atlases are an optimization, not a requirement.",
               3: "On-screen size comes from the transform you draw with, not from packing." } },

    { tag: "Architecture",
      q: "In an Entity Component System, where does behavior live?",
      choices: ["In systems that run over entities holding the relevant components", "In methods on each entity subclass", "In the components themselves", "In the rendering layer"],
      answer: 0,
      why: "ECS splits identity (entity), data (components) and logic (systems). A movement system iterates every entity that has Position and Velocity — which keeps related data contiguous in memory and avoids deep inheritance trees.",
      wrong: { 1: "That is the classic inheritance approach ECS is a reaction against — it leads to fragile hierarchies like <code>FlyingSwimmingEnemy</code>.",
               2: "In a strict ECS, components are plain data with no logic at all.",
               3: "Rendering is just one system among many; gameplay logic does not belong there." } },

    { tag: "Performance",
      q: "What problem does object pooling solve?",
      choices: ["Sprites overlapping incorrectly", "Allocation and garbage-collection spikes from rapidly created and destroyed objects", "Too many objects on screen at once", "Save files growing too large"],
      answer: 1,
      why: "Spawning and freeing hundreds of bullets per second churns memory and triggers GC pauses that show up as stutter. A pool pre-allocates them once and recycles inactive ones.",
      wrong: { 0: "Overlap order is depth sorting or z-index, a rendering concern.",
               2: "Pooling does not reduce what is on screen — it reduces the cost of managing it.",
               3: "Save size is about serialization, not runtime allocation." } },

    { tag: "AI",
      q: "What does the A* algorithm compute in a game?",
      choices: ["A shortest path from start to goal, guided by a heuristic", "A random wander pattern", "Field of view for an enemy", "Collision response between two bodies"],
      answer: 0,
      why: "A* explores nodes ordered by cost-so-far plus an estimate of cost-to-goal. That heuristic steers the search toward the target, so it finds the shortest path far faster than a blind flood fill.",
      wrong: { 1: "Wandering is usually noise or a simple steering behavior — no goal, no search.",
               2: "Field of view is a cone check plus a raycast for line of sight.",
               3: "Collision response is physics: resolving overlap and applying impulses." } },

    { tag: "AI",
      q: "An enemy cycles through Idle → Patrol → Chase → Attack. What pattern is that?",
      choices: ["A finite state machine", "A neural network", "A quadtree", "A shader graph"],
      answer: 0,
      why: "A finite state machine holds exactly one current state and moves between states on defined transitions. It is the standard way to keep enemy behavior readable and debuggable.",
      wrong: { 1: "A neural network learns weights from data — enormous overkill for four named behaviors.",
               2: "A quadtree is a spatial partition used to speed up collision and culling queries.",
               3: "A shader graph builds a visual effect on the GPU, not a behavior." } },

    { tag: "Coordinates",
      q: "In most 2D engines (and in screen space generally), which way does +y point?",
      choices: ["Up, as in math class", "Down, so (0,0) is the top-left corner", "It depends on the monitor", "There is no y axis in 2D"],
      answer: 1,
      why: "Screen coordinates inherit the raster scan order: origin at the top-left, y increasing downward. That is why &quot;jump&quot; is often a <em>negative</em> y velocity, and it trips up nearly everyone once.",
      wrong: { 0: "Math convention points +y up, and some engines (Godot's 3D, Unity's world space) follow it — but 2D screen space usually does not.",
               2: "The monitor plays no part; this is a software convention.",
               3: "2D has x and y by definition." } },

    { tag: "Physics",
      q: "Why do engines run physics on a <em>fixed</em> timestep while rendering at a variable rate?",
      choices: ["Physics is too slow to run every frame", "Fixed steps make the simulation deterministic and stable; variable steps cause tunneling and jitter", "The GPU cannot handle physics", "It saves battery"],
      answer: 1,
      why: "Integrators behave badly when <code>dt</code> swings around — a long frame can move a body straight through a wall. A fixed step (say 1/60 s), run as many times as needed to catch up, keeps results repeatable.",
      wrong: { 0: "Speed is not the issue; correctness is. Fixed stepping can mean running physics <em>more</em> often than you render.",
               2: "Physics normally runs on the CPU anyway, and GPU physics exists where wanted.",
               3: "Battery has nothing to do with the choice." } },

    { tag: "Engines",
      q: "Which pairing of engine and its primary scripting language is correct?",
      choices: ["Unity → C#", "Unity → C++", "Godot → Swift", "Unreal → Python"],
      answer: 0,
      why: "Unity scripts in C#. Godot's own language is GDScript (with C# supported), and Unreal uses C++ alongside Blueprints, its visual scripting system.",
      wrong: { 1: "Unity's engine core is C++, but the code <em>you</em> write is C#.",
               2: "Godot has no Swift binding. GDScript and C# are the supported choices.",
               3: "Unreal uses C++ and Blueprints. Python exists there only for editor tooling, not gameplay." } },

    { tag: "Collision",
      q: "What do collision layers and masks let you express?",
      choices: ["Which objects are drawn on top", "Which categories of object are allowed to collide with which others", "How heavy each object is", "The order physics runs in"],
      answer: 1,
      why: "A layer says what an object <em>is</em>; a mask says what it <em>looks for</em>. That is how player bullets pass through the player, hit enemies, and stop at walls — without a single <code>if</code> statement.",
      wrong: { 0: "Draw order is z-index or sorting layers, a separate rendering concept.",
               2: "Weight is mass, a rigidbody property.",
               3: "Step order is the engine's business; layers only filter which pairs are tested." } },

    { tag: "Rendering",
      q: "A fragment (pixel) shader runs —",
      choices: ["Once per object, on the CPU", "Once per candidate pixel, on the GPU", "Once per frame, on the CPU", "Once at load time"],
      answer: 1,
      why: "The GPU runs the fragment shader in parallel for every pixel the geometry covers, and its job is to output that pixel's color. A full-screen effect therefore runs it millions of times per frame.",
      wrong: { 0: "Shaders run on the GPU. The per-object stage is the vertex shader, and it is still GPU work.",
               2: "Per-frame CPU work is your game logic, not a shader.",
               3: "Shaders are <em>compiled</em> at load time but execute every frame." } },

    { tag: "Camera",
      q: "Which technique gives a camera that trails the player smoothly instead of snapping?",
      choices: ["Setting camera position to player position every frame", "Lerping the camera toward the player by a fraction each frame", "Disabling the camera during movement", "Rendering at a lower resolution"],
      answer: 1,
      why: "<code>cam = lerp(cam, target, t)</code> closes part of the remaining gap per frame, producing eased follow with a trailing feel. Scale <code>t</code> by delta time so it stays frame-rate independent.",
      wrong: { 0: "That is the hard snap the question asks you to avoid — it feels rigid and amplifies jitter.",
               2: "Disabling the camera would freeze the view entirely.",
               3: "Resolution affects sharpness and fill cost, not camera motion." } },

    { tag: "Networking",
      q: "In an authoritative-server multiplayer game, what is client-side prediction for?",
      choices: ["Guessing what the player will do next", "Moving the local player immediately instead of waiting a round trip, then reconciling with the server", "Predicting which players will disconnect", "Compressing packets"],
      answer: 1,
      why: "The client simulates its own input at once so controls feel instant, while the server stays the source of truth. When the server's state arrives, the client reconciles and replays any inputs made since.",
      wrong: { 0: "It predicts nothing about intent — it applies input the player has already given.",
               2: "Disconnect handling is a timeout, not prediction.",
               3: "Compression is a bandwidth technique, unrelated to latency hiding." } },

    { tag: "Game feel",
      q: "Screen shake, hit-stop and easing curves fall under which heading?",
      choices: ["Level design", "Game feel — the moment-to-moment responsiveness and punch of the controls", "Localization", "Build pipeline"],
      answer: 1,
      why: "Game feel (or &quot;juice&quot;) is the layer of feedback around mechanics: a brief freeze on impact, a small camera kick, eased motion. The rules do not change, but the game reads as far more satisfying.",
      wrong: { 0: "Level design shapes space and pacing — a different craft.",
               2: "Localization is translating and adapting content for other languages.",
               3: "The build pipeline is how the game is compiled and shipped." } },

    { tag: "Rendering",
      q: "What is a draw call?",
      choices: ["A request from the CPU to the GPU to render a batch of geometry", "One pixel being written to the screen", "A call to the game loop", "A physics query"],
      answer: 0,
      why: "Each draw call carries state and geometry across the CPU/GPU boundary. That crossing costs more than the drawing itself, which is why batching and atlasing pay off so heavily.",
      wrong: { 1: "That is a fragment write, done inside a draw call — many millions of them.",
               2: "The game loop is CPU control flow, not a GPU submission.",
               3: "A physics query, like a raycast, has nothing to do with rendering." } },

    { tag: "Spatial",
      q: "Why use a quadtree or spatial hash for collision detection?",
      choices: ["It makes collisions more accurate", "It avoids testing every object against every other — cutting O(n²) checks to something far smaller", "It renders objects faster", "It compresses the level file"],
      answer: 1,
      why: "Naive collision tests every pair: 1,000 objects is about 500,000 checks per frame. A spatial structure limits tests to objects sharing a cell or region, so only genuine neighbors are compared.",
      wrong: { 0: "The per-pair math is identical. The structure only decides which pairs are worth testing.",
               2: "Culling structures can help rendering too, but the question is about collision cost.",
               3: "Level file size is a serialization concern." } }
  ]
});
