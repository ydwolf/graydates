GD.addTopic({
  id: "git",
  name: "Git & Version Control",
  sigil: "git",
  blurb: "Commits, branches, and undoing mistakes",
  questions: [
    { tag: "Basics",
      q: "What is the difference between <code>git commit</code> and <code>git push</code>?",
      choices: ["They do the same thing", "<code>commit</code> records a snapshot locally; <code>push</code> uploads local commits to a remote", "<code>commit</code> uploads; <code>push</code> saves locally", "<code>push</code> only works on the main branch"],
      answer: 1,
      why: "Git is distributed: committing writes history into your own repository, and nothing leaves your machine until you push. That is why you can commit on a plane.",
      wrong: { 0: "You can commit fifty times before pushing once — they are separate operations.",
               2: "Exactly reversed.",
               3: "You can push any branch to any remote you have access to." } },

    { tag: "Staging",
      q: "What does <code>git add file.py</code> actually do?",
      choices: ["Commits the file", "Stages the current contents of the file for the next commit", "Adds the file to <code>.gitignore</code>", "Uploads the file"],
      answer: 1,
      why: "<code>add</code> copies the file's current state into the staging area (the index). The commit then captures whatever is staged — which is how you commit some of your changes and leave the rest.",
      wrong: { 0: "Committing is a second, separate step. Staging only prepares it.",
               2: "That is the opposite — <code>.gitignore</code> keeps files <em>out</em> of Git.",
               3: "Nothing goes to a server until <code>push</code>." } },

    { tag: "Staging",
      q: "You edit a file after staging it, then commit. What gets committed?",
      choices: ["The newest version on disk", "The version as it was when you ran <code>git add</code>", "Both versions", "Nothing — Git errors out"],
      answer: 1,
      why: "The index holds a snapshot taken at <code>add</code> time. Later edits sit in the working tree, unstaged, and the file shows up in <em>both</em> sections of <code>git status</code> until you add it again.",
      wrong: { 0: "Git commits the index, not the working directory — a distinction that surprises people regularly.",
               2: "A commit stores exactly one version of each file.",
               3: "It succeeds silently, which is what makes this worth knowing." } },

    { tag: "Remotes",
      q: "What is the difference between <code>git fetch</code> and <code>git pull</code>?",
      choices: ["<code>fetch</code> downloads remote changes without touching your branch; <code>pull</code> is fetch plus merge", "<code>fetch</code> uploads, <code>pull</code> downloads", "They are identical", "<code>fetch</code> only works once per day"],
      answer: 0,
      why: "<code>fetch</code> updates your remote-tracking branches so you can inspect what changed. <code>pull</code> goes further and merges (or rebases) it into your current branch immediately.",
      wrong: { 1: "Neither uploads. Uploading is <code>push</code>.",
               2: "Only <code>pull</code> modifies your working branch — which is exactly when conflicts appear.",
               3: "Fetch as often as you like." } },

    { tag: "Branches",
      q: "What is a Git branch, underneath?",
      choices: ["A full copy of the project directory", "A movable pointer to a commit", "A folder on the remote server", "A compressed archive"],
      answer: 1,
      why: "A branch is a 41-byte file containing a commit hash. Committing moves the pointer forward, which is why creating a branch is instantaneous no matter how large the repository is.",
      wrong: { 0: "Nothing is duplicated. That misconception comes from older systems like SVN.",
               2: "Branches exist locally first and may never be pushed anywhere.",
               3: "Git compresses objects, but a branch is only a reference to one of them." } },

    { tag: "Merging",
      q: "What causes a merge conflict?",
      choices: ["Two branches changed the same lines of the same file differently", "Two branches changed different files", "You forgot to push", "The remote is offline"],
      answer: 0,
      why: "Git merges automatically whenever changes do not overlap. When two branches edit the same region, it cannot know which version wins and hands the decision to you with conflict markers.",
      wrong: { 1: "Different files merge cleanly without a word.",
               2: "Conflicts arise from overlapping content, not from when you pushed.",
               3: "Merging is entirely local and needs no network." } },

    { tag: "Config",
      q: "What does <code>.gitignore</code> do?",
      choices: ["Deletes the listed files", "Tells Git not to track files matching those patterns", "Hides files from your editor", "Encrypts the listed files"],
      answer: 1,
      why: "It keeps build output, <code>node_modules</code> and secrets out of history. Note that it only affects <em>untracked</em> files — anything already committed keeps being tracked until you remove it explicitly.",
      wrong: { 0: "The files stay on your disk exactly where they are.",
               2: "Your editor's file tree is unaffected.",
               3: "Git does no encryption. A committed secret must be treated as leaked." } },

    { tag: "History",
      q: "What does HEAD refer to?",
      choices: ["The first commit in the repository", "A pointer to the commit you currently have checked out", "The remote's latest commit", "The largest file"],
      answer: 1,
      why: "HEAD marks where you are now — normally by pointing at a branch, which in turn points at a commit. That is what makes <code>HEAD~1</code> mean &quot;one commit back from here.&quot;",
      wrong: { 0: "The first commit is the root, reachable with <code>git log --reverse</code>.",
               2: "The remote's tip is <code>origin/main</code> and can be far ahead of or behind HEAD.",
               3: "HEAD is about position in history, not file size." } },

    { tag: "Undo",
      q: "A bad commit is already pushed and others have pulled it. What is the safe way to undo it?",
      choices: ["<code>git reset --hard</code> and force push", "<code>git revert &lt;commit&gt;</code>, which adds a new commit undoing the changes", "Delete the repository and re-clone", "Edit the commit in GitHub's web UI"],
      answer: 1,
      why: "<code>revert</code> is additive: history stays intact and everyone's clone stays consistent. <code>reset</code> rewrites history, which is fine locally but hostile once others have the commits.",
      wrong: { 0: "Force-pushing over commits teammates already have will break their branches and can destroy their work.",
               2: "Re-cloning does not undo anything — the bad commit is still on the remote.",
               3: "The web UI cannot rewrite a pushed commit either; it would make a new one, which is what revert does properly." } },

    { tag: "Undo",
      q: "What is the difference between <code>git reset</code> and <code>git revert</code>?",
      choices: ["<code>reset</code> moves the branch pointer, rewriting history; <code>revert</code> adds a new commit that undoes an old one", "They are the same", "<code>revert</code> deletes the repository", "<code>reset</code> only works on remotes"],
      answer: 0,
      why: "<code>reset</code> rewinds your branch to another commit — clean for local work you have not shared. <code>revert</code> leaves history alone and appends the inverse change, which is safe for shared branches.",
      wrong: { 1: "One rewrites history and one extends it — nearly opposite in effect on collaborators.",
               2: "Nothing is deleted; revert only adds a commit.",
               3: "<code>reset</code> is purely local. It never touches a remote." } },

    { tag: "Rebase",
      q: "How does <code>git rebase main</code> differ from <code>git merge main</code>?",
      choices: ["Rebase replays your commits on top of main, producing linear history; merge creates a merge commit joining both", "Rebase deletes main", "Merge is faster", "They produce identical history"],
      answer: 0,
      why: "Merge preserves the true shape of what happened. Rebase rewrites your commits onto a new base for a straight-line history — which is why the rule is never to rebase commits others have already pulled.",
      wrong: { 1: "<code>main</code> is untouched; only your branch's commits are replayed.",
               2: "Speed is not the distinction, and rebase can take longer with conflicts to resolve per commit.",
               3: "One is linear, the other has a merge commit and two parents — visibly different graphs." } },

    { tag: "Stash",
      q: "What is <code>git stash</code> for?",
      choices: ["Permanently deleting changes", "Shelving uncommitted changes so you can switch context, then restoring them later", "Stashing files on the remote", "Compressing the repository"],
      answer: 1,
      why: "It tucks your working-tree and staged changes onto a stack and gives you a clean tree. <code>git stash pop</code> brings them back — ideal when an urgent fix interrupts half-finished work.",
      wrong: { 0: "Nothing is destroyed; the changes go on the stash stack.",
               2: "Stashes are local and are not pushed.",
               3: "Repository compression is <code>git gc</code>." } },

    { tag: "Inspection",
      q: "What does plain <code>git diff</code> show?",
      choices: ["Changes between the working tree and the staging area", "Changes between the last two commits", "Every change ever made", "Differences between branches"],
      answer: 0,
      why: "Bare <code>git diff</code> shows what you have changed but not yet staged. <code>git diff --staged</code> shows what is staged and about to be committed.",
      wrong: { 1: "That would be <code>git diff HEAD~1 HEAD</code>.",
               2: "Full history is <code>git log -p</code>.",
               3: "Comparing branches takes explicit names, as in <code>git diff main..feature</code>." } },

    { tag: "Remotes",
      q: "What is <code>origin</code>?",
      choices: ["The first commit", "The conventional default name for the remote you cloned from", "The main branch", "A Git command"],
      answer: 1,
      why: "<code>origin</code> is just a nickname for a URL, set automatically by <code>clone</code>. Rename it or add more — <code>upstream</code> alongside <code>origin</code> is the standard fork setup.",
      wrong: { 0: "The first commit is the root commit and has no special name.",
               2: "The main branch is <code>main</code> or <code>master</code>. <code>origin/main</code> combines the two ideas.",
               3: "It is an argument to commands, not a command itself." } },

    { tag: "Commits",
      q: "What is a commit hash like <code>a3f5c9d</code>?",
      choices: ["A random ID", "A SHA-1 checksum of the commit's content and metadata", "The line count of the commit", "A timestamp in hex"],
      answer: 1,
      why: "The hash is computed from the tree, the parent commit, the author and the message. Change any of it and you get a different hash — which is why rebasing produces new IDs, and why history is tamper-evident.",
      wrong: { 0: "It is fully deterministic. Identical content and metadata produce the identical hash.",
               2: "Size plays no part in the hash.",
               3: "The timestamp is one input among several, not the whole value." } },

    { tag: "Collaboration",
      q: "In the GitHub workflow, what is a pull request?",
      choices: ["A request to download the repository", "A proposal to merge one branch into another, opened for review and discussion", "A way to pull changes faster", "A backup of your branch"],
      answer: 1,
      why: "A PR wraps a branch in review: a diff, comments, CI checks and approvals, all before anything lands on the main branch. It is a platform feature layered on top of plain Git.",
      wrong: { 0: "Downloading is <code>clone</code>.",
               2: "It is not a performance feature at all.",
               3: "Pushing the branch is the backup. The PR is the review conversation." } },

    { tag: "Branches",
      q: "Which command creates a new branch and switches to it in one step?",
      choices: ["<code>git branch feature</code>", "<code>git checkout -b feature</code>", "<code>git merge feature</code>", "<code>git clone feature</code>"],
      answer: 1,
      why: "<code>checkout -b</code> creates and switches together. Modern Git also offers the clearer <code>git switch -c feature</code>.",
      wrong: { 0: "<code>git branch</code> creates it but leaves you where you are.",
               2: "<code>merge</code> combines an existing branch into the current one.",
               3: "<code>clone</code> copies a whole repository from a URL." } },

    { tag: "Danger",
      q: "Why is <code>git push --force</code> dangerous on a shared branch?",
      choices: ["It uses a lot of bandwidth", "It can overwrite commits others have pushed, erasing their work from the remote", "It logs you out", "It makes the repository read-only"],
      answer: 1,
      why: "A force push replaces the remote branch with yours, dropping anything you did not have. On a shared branch use <code>--force-with-lease</code>, which refuses if the remote moved since you last looked.",
      wrong: { 0: "It usually transfers less data than a normal push, not more.",
               2: "Authentication is untouched.",
               3: "Permissions do not change; only history does." } },

    { tag: "Inspection",
      q: "What does <code>git status</code> tell you?",
      choices: ["The commit history", "Your current branch, staged changes, unstaged changes and untracked files", "Who owns the repository", "The size of the repository"],
      answer: 1,
      why: "It is the orientation command — where you are, what is staged, what is modified, what Git is not tracking yet. Run it before every commit.",
      wrong: { 0: "History is <code>git log</code>.",
               2: "Ownership is a hosting-platform concept, not something Git records.",
               3: "Size comes from <code>git count-objects -vH</code>." } },

    { tag: "Collaboration",
      q: "How does a fork differ from a branch?",
      choices: ["A fork is your own server-side copy of the whole repository; a branch is a line of development inside one repository", "A fork is faster", "A branch requires a GitHub account", "They are the same thing"],
      answer: 0,
      why: "Forking gives you a repository you own — the standard route for contributing to a project you cannot push to. Branching happens inside a repository you already have write access to.",
      wrong: { 1: "Branching is the instant operation; forking copies a repository.",
               2: "Branches are pure Git and work with no account or network at all.",
               3: "One is a whole-repository copy on a host, the other a pointer inside a repository." } }
  ]
});
