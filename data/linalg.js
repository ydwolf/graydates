GD.addTopic({
  id: "linalg",
  name: "Linear Algebra",
  sigil: "[A]",
  blurb: "Matrices, vectors, and what they actually do",
  questions: [
    { tag: "Products",
      q: "If <code>A</code> is 3×4 and <code>B</code> is 4×2, what are the dimensions of <code>AB</code>?",
      choices: ["3×2", "4×4", "2×3", "Undefined"],
      answer: 0,
      why: "The inner dimensions must match (4 and 4 — they do), and the result takes the outer ones: 3 rows from <code>A</code>, 2 columns from <code>B</code>.",
      wrong: { 1: "4×4 would come from multiplying something 4×k by something k×4.",
               2: "That is the shape of <code>(AB)ᵀ</code>, or of <code>BᵀAᵀ</code> — not of <code>AB</code>.",
               3: "It is perfectly defined. <code>BA</code>, on the other hand, would not be: 2 does not match 3." } },

    { tag: "Products",
      q: "For square matrices, is <code>AB = BA</code>?",
      choices: ["Always", "Never", "Not in general — matrix multiplication is not commutative", "Only if both are invertible"],
      answer: 2,
      why: "Order matters. Multiplying by a matrix applies a transformation, and rotating then scaling is not the same as scaling then rotating. Some special pairs do commute — with the identity, or with their own inverse.",
      wrong: { 0: "A single 2×2 counterexample settles it. Commutativity is the exception, not the rule.",
               1: "&quot;Never&quot; is too strong: <code>AI = IA</code> and <code>AA⁻¹ = A⁻¹A</code> always hold.",
               3: "Invertibility does not buy commutativity. Two invertible rotations in 3D generally do not commute." } },

    { tag: "Identity",
      q: "What is the identity matrix <code>I</code>?",
      choices: ["A matrix of all ones", "A square matrix with ones on the diagonal and zeros elsewhere", "A matrix of all zeros", "Any matrix whose determinant is 1"],
      answer: 1,
      why: "<code>I</code> leaves every vector unchanged: <code>Ix = x</code> and <code>AI = IA = A</code>. It is the multiplicative identity, the matrix equivalent of the number 1.",
      wrong: { 0: "An all-ones matrix maps every vector to a multiple of the all-ones vector — nothing like the identity.",
               2: "The zero matrix collapses everything to the origin. That is the <em>additive</em> identity.",
               3: "Plenty of non-identity matrices have determinant 1, such as any rotation or shear." } },

    { tag: "Determinants",
      q: "What is the determinant of <code>[[a, b], [c, d]]</code>?",
      choices: ["<code>ad − bc</code>", "<code>ab − cd</code>", "<code>ac − bd</code>", "<code>a + d</code>"],
      answer: 0,
      why: "For a 2×2 matrix the determinant is <code>ad − bc</code>: the product of the main diagonal minus the product of the anti-diagonal.",
      wrong: { 1: "That multiplies the top row and the bottom row against themselves — not a determinant.",
               2: "That pairs the columns instead of the diagonals.",
               3: "<code>a + d</code> is the <em>trace</em>, the sum of the diagonal, which is a different quantity." } },

    { tag: "Determinants",
      q: "A square matrix has determinant 0. What does that tell you?",
      choices: ["It is the identity matrix", "It is singular — it has no inverse, and its columns are linearly dependent", "It is symmetric", "All its entries are 0"],
      answer: 1,
      why: "A zero determinant means the transformation collapses space into a lower dimension — area or volume goes to zero. Information is destroyed, so nothing can undo it, and no inverse exists.",
      wrong: { 0: "The identity has determinant 1 and is as invertible as a matrix can be.",
               2: "Symmetry means <code>A = Aᵀ</code> and says nothing about the determinant.",
               3: "A matrix such as <code>[[1, 1], [1, 1]]</code> has determinant 0 with no zero entry at all." } },

    { tag: "Vectors",
      q: "Two nonzero vectors have a dot product of 0. What follows?",
      choices: ["They are parallel", "They are perpendicular (orthogonal)", "They are equal", "One of them is the zero vector"],
      answer: 1,
      why: "Since <code>u · v = |u||v| cos θ</code>, a zero dot product with nonzero lengths forces <code>cos θ = 0</code> — the vectors meet at 90°.",
      wrong: { 0: "Parallel vectors have the <em>largest</em> possible dot product for their lengths, since cos 0 = 1.",
               2: "Equal nonzero vectors give <code>|v|²</code>, which is strictly positive.",
               3: "The question states both are nonzero — that is exactly what makes the conclusion interesting." } },

    { tag: "Operations",
      q: "What does the transpose <code>Aᵀ</code> do?",
      choices: ["Inverts the matrix", "Reflects it across the main diagonal, swapping rows and columns", "Negates every entry", "Sorts the rows"],
      answer: 1,
      why: "Entry <code>(i, j)</code> moves to <code>(j, i)</code>, so an m×n matrix becomes n×m. Note that <code>(AB)ᵀ = BᵀAᵀ</code> — the order reverses.",
      wrong: { 0: "The inverse is <code>A⁻¹</code>, which is far more work to compute. They coincide only for orthogonal matrices.",
               2: "Negation is <code>−A</code>, an entirely different operation.",
               3: "No reordering happens; the positions are reflected by a fixed rule." } },

    { tag: "Eigen",
      q: "<code>v</code> is an eigenvector of <code>A</code> with eigenvalue <code>λ</code>. What equation holds?",
      choices: ["<code>Av = λv</code>", "<code>Av = 0</code>", "<code>A = λv</code>", "<code>vA = λ</code>"],
      answer: 0,
      why: "Multiplying by <code>A</code> leaves an eigenvector pointing along the same line and merely scales it by <code>λ</code>. Those directions are the axes the transformation does not rotate.",
      wrong: { 1: "That describes a null-space vector — the special case where <code>λ = 0</code>.",
               2: "This is dimensionally nonsense: a matrix cannot equal a scaled vector.",
               3: "Multiplying a row vector on the left is a different operation, and the result is a vector, not a scalar." } },

    { tag: "Rank",
      q: "What does the rank of a matrix measure?",
      choices: ["The number of rows", "The number of linearly independent columns (equivalently, rows)", "The largest entry", "The number of nonzero entries"],
      answer: 1,
      why: "Rank is the dimension of the space the columns actually span — how many genuinely independent directions survive the transformation. Remarkably, row rank and column rank always agree.",
      wrong: { 0: "A matrix with 100 rows can have rank 1 if every row is a multiple of one row.",
               2: "Rank has nothing to do with magnitudes, only with independence.",
               3: "<code>[[1, 1], [1, 1]]</code> has four nonzero entries and rank 1." } },

    { tag: "Independence",
      q: "A set of vectors is <em>linearly dependent</em> when —",
      choices: ["They all have the same length", "At least one can be written as a combination of the others", "They are all perpendicular", "There are more of them than the dimension allows"],
      answer: 1,
      why: "Dependence means one vector adds no new direction — it already lies in the span of the rest. Equivalently, some nontrivial combination of them sums to the zero vector.",
      wrong: { 0: "Length is irrelevant. Two vectors of different lengths pointing the same way are dependent.",
               2: "Nonzero perpendicular vectors are automatically <em>in</em>dependent.",
               3: "That does force dependence — any 3 vectors in R² must be dependent — but it is a symptom, not the definition." } },

    { tag: "Systems",
      q: "For a square system <code>Ax = b</code>, when is there exactly one solution for every <code>b</code>?",
      choices: ["When <code>A</code> is invertible", "When <code>b</code> is the zero vector", "When <code>A</code> is symmetric", "Always"],
      answer: 0,
      why: "If <code>A⁻¹</code> exists, then <code>x = A⁻¹b</code> is the unique answer. Equivalently: nonzero determinant, full rank, independent columns — these all say the same thing.",
      wrong: { 1: "With <code>b = 0</code> there is always the trivial solution <code>x = 0</code>, and if <code>A</code> is singular there are infinitely many others.",
               2: "Symmetry does not prevent singularity — the all-zeros matrix is symmetric.",
               3: "A singular system has either no solution or infinitely many, never exactly one." } },

    { tag: "Systems",
      q: "What is Gaussian elimination used for?",
      choices: ["Computing eigenvalues directly", "Row-reducing a matrix to solve systems, find rank, or compute an inverse", "Normalizing a vector", "Transposing a matrix in place"],
      answer: 1,
      why: "Systematic row operations drive the matrix to echelon form, where the system can be read off by back-substitution. The same procedure yields rank, determinant and inverses.",
      wrong: { 0: "Eigenvalues come from the characteristic polynomial or from iterative numerical methods.",
               2: "Normalizing is dividing a single vector by its own length.",
               3: "Transposition is a rearrangement, requiring no arithmetic at all." } },

    { tag: "Subspaces",
      q: "The null space (kernel) of <code>A</code> is the set of all vectors <code>x</code> such that —",
      choices: ["<code>Ax = b</code>", "<code>Ax = 0</code>", "<code>Ax = x</code>", "<code>x = 0</code>"],
      answer: 1,
      why: "The null space collects everything the transformation crushes to the origin. It contains only the zero vector exactly when the columns are independent.",
      wrong: { 0: "That is the general solution set of an inhomogeneous system — not a subspace unless <code>b = 0</code>.",
               2: "<code>Ax = x</code> describes eigenvectors with eigenvalue 1.",
               3: "The zero vector is always in the null space, but for a singular matrix it is not the only member." } },

    { tag: "Vectors",
      q: "What is the magnitude of the vector <code>(3, 4)</code>?",
      choices: ["7", "5", "12", "25"],
      answer: 1,
      why: "The norm is <code>√(3² + 4²) = √25 = 5</code> — the Pythagorean theorem, extended to any number of dimensions.",
      wrong: { 0: "7 is the sum of the components, sometimes called the taxicab (L1) norm — a different measure.",
               2: "12 is the product of the components, which is not a length.",
               3: "25 is the sum of the squares. You still have to take the square root." } },

    { tag: "Vectors",
      q: "What does the cross product <code>u × v</code> produce?",
      choices: ["A scalar", "A vector perpendicular to both, defined only in 3D", "The angle between them", "A matrix"],
      answer: 1,
      why: "The cross product returns a vector orthogonal to both inputs, with magnitude equal to the area of the parallelogram they span. It is specific to three dimensions.",
      wrong: { 0: "The <em>dot</em> product returns a scalar. The cross product returns a vector — that is the whole distinction.",
               2: "You can recover the angle from either product, but neither returns it directly.",
               3: "There is a matrix formulation of it, but the result of the operation is a vector." } },

    { tag: "Operations",
      q: "What is the trace of a square matrix?",
      choices: ["The sum of the entries on the main diagonal", "The product of all entries", "The largest eigenvalue", "The number of rows"],
      answer: 0,
      why: "The trace sums the diagonal. Pleasingly, it also equals the sum of the eigenvalues, and <code>tr(AB) = tr(BA)</code> even when <code>AB ≠ BA</code>.",
      wrong: { 1: "The product of the eigenvalues is the <em>determinant</em>, and it is not the product of the entries.",
               2: "The largest eigenvalue is the spectral radius. The trace is the sum of all of them.",
               3: "That is just the dimension, which says nothing about the entries." } },

    { tag: "Orthogonality",
      q: "A matrix <code>Q</code> is orthogonal. Which is true?",
      choices: ["<code>Q</code> is symmetric", "<code>QᵀQ = I</code>, so <code>Q⁻¹ = Qᵀ</code>", "<code>Q</code> has determinant 0", "Every entry of <code>Q</code> is 0 or 1"],
      answer: 1,
      why: "Its columns are orthonormal, which makes the transpose the inverse — a cheap inverse, and one reason rotation matrices are so pleasant to work with. Orthogonal maps preserve lengths and angles.",
      wrong: { 0: "Rotation matrices are orthogonal but not symmetric in general.",
               2: "An orthogonal matrix has determinant +1 or −1, never 0 — it is always invertible.",
               3: "Rotations are full of irrational entries like <code>cos 30°</code>. That description fits permutation matrices, a special case." } },

    { tag: "Transformations",
      q: "Geometrically, what does multiplying a vector by a matrix do?",
      choices: ["Applies a linear transformation — rotation, scaling, shear, projection, or a mix", "Always rotates it", "Sorts its components", "Adds a constant to each component"],
      answer: 0,
      why: "Every matrix <em>is</em> a linear transformation, and its columns are just where the basis vectors land. Rotation, reflection, scaling and projection are all special cases.",
      wrong: { 1: "Rotation is one particular family. A diagonal matrix scales without rotating at all.",
               2: "Sorting is not linear and no matrix performs it.",
               3: "Adding a constant is a <em>translation</em>, which is not linear — it needs homogeneous coordinates, which is exactly why graphics uses 4×4 matrices for 3D." } },

    { tag: "Basis",
      q: "How many vectors are in a basis for <code>Rⁿ</code>?",
      choices: ["Any number, as long as they span", "Exactly <code>n</code>", "<code>n − 1</code>", "<code>n²</code>"],
      answer: 1,
      why: "A basis must both span the space and be linearly independent, and those two demands pin the count to exactly <code>n</code>. Fewer cannot span; more cannot stay independent.",
      wrong: { 0: "A spanning set can be too big — then it is dependent and is not a basis.",
               2: "<code>n − 1</code> independent vectors span only a hyperplane, missing a whole direction.",
               3: "<code>n²</code> is the number of entries in an n×n matrix, not the dimension of the space." } },

    { tag: "Determinants",
      q: "Which identity always holds for square matrices of the same size?",
      choices: ["<code>det(A + B) = det(A) + det(B)</code>", "<code>det(AB) = det(A) · det(B)</code>", "<code>det(Aᵀ) = −det(A)</code>", "<code>det(2A) = 2 · det(A)</code>"],
      answer: 1,
      why: "Determinants are multiplicative. It makes sense geometrically: if <code>A</code> triples volume and <code>B</code> doubles it, doing both multiplies volume by six.",
      wrong: { 0: "Determinants are emphatically not additive. Take <code>A = I</code> and <code>B = −I</code> in 2D: the left side is 0, the right side is 2.",
               2: "Transposing leaves the determinant unchanged: <code>det(Aᵀ) = det(A)</code>.",
               3: "Scaling an n×n matrix scales the determinant by <code>2ⁿ</code>, because every one of the n dimensions is stretched." } }
  ]
});
