/* Each question: tag, q, choices[4], answer (0-based index), why (the right one),
   wrong { choiceIndex: "why that one is wrong" } */
GD.addTopic({
  id: "python",
  name: "Python",
  sigil: ".py",
  blurb: "Lists, dicts, and the gotchas that bite",
  questions: [
    { tag: "Lists",
      q: "How do you add <code>5</code> to list <code>a</code> in place?",
      choices: ["<code>a.push(5)</code>", "<code>a.add(5)</code>", "<code>a += 5</code>", "<code>a.append(5)</code>"],
      answer: 3,
      why: "<code>append</code> mutates the existing list, putting one new item on the end. The list object itself changes and nothing new is returned.",
      wrong: { 0: "<code>push</code> is JavaScript's array method. Python lists have no <code>push</code>, so this raises <code>AttributeError</code>.",
               1: "<code>add</code> belongs to <code>set</code>, not <code>list</code>. Calling it on a list raises <code>AttributeError</code>.",
               2: "<code>+=</code> on a list extends it with an <em>iterable</em>. An int is not iterable, so this raises <code>TypeError</code>. <code>a += [5]</code> would work." } },

    { tag: "Slicing",
      q: "What does <code>a[::-1]</code> give you?",
      choices: ["A reversed copy of <code>a</code>", "<code>a</code> reversed in place, returning <code>None</code>", "The last element of <code>a</code>", "An error — the step cannot be negative"],
      answer: 0,
      why: "A slice with step <code>-1</code> walks the sequence backwards and builds a <strong>new</strong> list. The original <code>a</code> is untouched.",
      wrong: { 1: "That describes <code>a.reverse()</code>, which mutates in place and returns <code>None</code>. Slicing never mutates.",
               2: "That is <code>a[-1]</code>. The two colons make this a slice of the whole sequence, not a single index.",
               3: "Negative steps are perfectly legal — that is exactly how you reverse a sequence." } },

    { tag: "Tuples",
      q: "<code>t = (1, 2, 3)</code>. What happens on <code>t[0] = 9</code>?",
      choices: ["<code>t</code> becomes <code>(9, 2, 3)</code>", "<code>TypeError</code> — tuples are immutable", "A new tuple is returned", "<code>IndexError</code>"],
      answer: 1,
      why: "Tuples are immutable: once built, their slots cannot be reassigned. Python raises <code>TypeError: &#39;tuple&#39; object does not support item assignment</code>.",
      wrong: { 0: "That is list behavior. A tuple cannot be edited after creation — that immutability is the whole point of the type.",
               2: "Assignment statements never return a value, and there is no hidden copy. The line simply raises.",
               3: "Index 0 exists and is perfectly valid. The failure is about mutation, not the index." } },

    { tag: "Dicts",
      q: "<code>d = {&quot;a&quot;: 1}</code>. Which expression returns <code>0</code> instead of raising when the key is missing?",
      choices: ["<code>d[&quot;b&quot;]</code>", "<code>d.get(&quot;b&quot;, 0)</code>", "<code>d.fetch(&quot;b&quot;, 0)</code>", "<code>d.get(&quot;b&quot;)</code>"],
      answer: 1,
      why: "<code>dict.get(key, default)</code> looks the key up and hands back the default if it is absent, instead of raising.",
      wrong: { 0: "Square brackets on a missing key raise <code>KeyError</code>. That is the behavior the question is asking you to avoid.",
               2: "There is no <code>fetch</code> method on <code>dict</code>. This raises <code>AttributeError</code>.",
               3: "<code>get</code> with no default returns <code>None</code>, not <code>0</code>. Close, but the wrong falsy value." } },

    { tag: "Identity",
      q: "What is the difference between <code>==</code> and <code>is</code>?",
      choices: ["No difference — <code>is</code> is just older syntax", "<code>==</code> compares values; <code>is</code> compares object identity", "<code>==</code> compares identity; <code>is</code> compares values", "<code>is</code> only works on numbers"],
      answer: 1,
      why: "<code>==</code> asks &quot;do these hold the same value?&quot; and calls <code>__eq__</code>. <code>is</code> asks &quot;are these literally the same object in memory?&quot; Two equal lists are <code>==</code> but not <code>is</code>.",
      wrong: { 0: "They are separate operators with different results. Using <code>is</code> to compare values is a classic source of bugs.",
               2: "Backwards. <code>is</code> is the identity check.",
               3: "<code>is</code> works on any object. It is most correctly used with singletons like <code>None</code>: <code>if x is None</code>." } },

    { tag: "Functions",
      q: "Why is <code>def f(items=[]):</code> considered a bug?",
      choices: ["A list cannot be a default value", "The default list is created once and shared by every call", "It is slower than <code>items=list()</code>", "Python copies the list on each call, wasting memory"],
      answer: 1,
      why: "Default arguments are evaluated once, when the <code>def</code> runs. Every call that omits <code>items</code> gets the <em>same</em> list, so appends leak from one call into the next. Use <code>items=None</code> and build the list inside.",
      wrong: { 0: "It is syntactically legal — that is exactly why the bug is easy to miss.",
               2: "They behave identically in speed. The problem is shared state, not performance.",
               3: "The opposite: Python does <em>not</em> copy it. One list is reused forever." } },

    { tag: "Comprehensions",
      q: "What is <code>[x * x for x in range(4)]</code>?",
      choices: ["<code>[1, 4, 9, 16]</code>", "<code>[0, 1, 4, 9]</code>", "<code>[0, 1, 4, 9, 16]</code>", "<code>[0, 2, 4, 6]</code>"],
      answer: 1,
      why: "<code>range(4)</code> yields 0, 1, 2, 3 — starting at zero and stopping <em>before</em> 4. Squaring each gives 0, 1, 4, 9.",
      wrong: { 0: "That is <code>range(1, 5)</code>. <code>range(4)</code> starts at 0, not 1.",
               2: "Five items would need five inputs. <code>range(4)</code> produces exactly four.",
               3: "Those are doubles, not squares — that would be <code>x * 2</code>." } },

    { tag: "Numbers",
      q: "What is <code>-7 // 2</code> in Python 3?",
      choices: ["<code>-3</code>", "<code>-4</code>", "<code>-3.5</code>", "<code>3</code>"],
      answer: 1,
      why: "<code>//</code> floors — it rounds toward negative infinity, not toward zero. −3.5 floored is −4. (C and Java would truncate to −3.)",
      wrong: { 0: "That is truncation toward zero, which is what many other languages do. Python deliberately floors instead.",
               2: "<code>//</code> always returns a whole number for int operands. <code>/</code> would give <code>-3.5</code>.",
               3: "The sign is preserved — a negative divided by a positive stays negative." } },

    { tag: "Sorting",
      q: "What does <code>a.sort()</code> return?",
      choices: ["A new sorted list", "<code>None</code> — it sorts <code>a</code> in place", "The first element", "A sorted copy, leaving <code>a</code> alone"],
      answer: 1,
      why: "<code>list.sort()</code> rearranges the list in place and returns <code>None</code>. If you want a sorted copy, use <code>sorted(a)</code>. Writing <code>a = a.sort()</code> silently destroys your list.",
      wrong: { 0: "That is <code>sorted(a)</code>. The in-place method deliberately returns nothing so you do not chain it by accident.",
               2: "It returns no element at all. <code>None</code> is the sentinel for &quot;nothing to hand back.&quot;",
               3: "<code>sort</code> definitely changes <code>a</code>. The non-mutating version is the built-in <code>sorted</code>." } },

    { tag: "Sets",
      q: "What is <code>len(set([1, 2, 2, 3, 3, 3]))</code>?",
      choices: ["<code>6</code>", "<code>3</code>", "<code>2</code>", "<code>1</code>"],
      answer: 1,
      why: "A set holds only distinct elements, so the duplicates collapse to <code>{1, 2, 3}</code> — three items.",
      wrong: { 0: "6 is the length of the original list. Converting to a set is the standard way to drop duplicates.",
               2: "Three distinct values survive: 1, 2 and 3.",
               3: "Only identical values merge. 1, 2 and 3 are all different." } },

    { tag: "Strings",
      q: "Given <code>name = &quot;Wolf&quot;</code>, which prints <code>Hello, Wolf!</code>?",
      choices: ["<code>print(&quot;Hello, name!&quot;)</code>", "<code>print(f&quot;Hello, {name}!&quot;)</code>", "<code>print(&quot;Hello, $name!&quot;)</code>", "<code>print(&quot;Hello, &quot; + name)</code>"],
      answer: 1,
      why: "An f-string evaluates whatever is inside the braces and drops the result into the text. Any expression works there, not just a bare variable.",
      wrong: { 0: "Without the <code>f</code> prefix this is plain text. It prints the literal word <code>name</code>.",
               2: "<code>$name</code> is shell and PHP interpolation. Python does nothing special with a dollar sign in a string.",
               3: "This is valid Python but prints <code>Hello, Wolf</code> — the exclamation mark is missing." } },

    { tag: "Functions",
      q: "In <code>def f(*args, **kwargs):</code>, what do the two parameters collect?",
      choices: ["<code>args</code> is a tuple of positional arguments; <code>kwargs</code> is a dict of keyword arguments", "<code>args</code> is a list of keywords; <code>kwargs</code> is a tuple", "Both collect positional arguments", "They are C-style pointers"],
      answer: 0,
      why: "One star packs any extra positional arguments into a tuple; two stars pack any extra <code>name=value</code> arguments into a dict. Together they let a function take anything.",
      wrong: { 1: "Reversed, and the types are wrong: <code>args</code> is a tuple of positionals, <code>kwargs</code> a dict of keywords.",
               2: "Only <code>*args</code> takes positionals. <code>**kwargs</code> requires names.",
               3: "Python has no pointers. The stars are packing and unpacking syntax." } },

    { tag: "Loops",
      q: "What does <code>for i, v in enumerate([&quot;a&quot;, &quot;b&quot;])</code> bind on the first pass?",
      choices: ["<code>i = &quot;a&quot;, v = &quot;b&quot;</code>", "<code>i = 0, v = &quot;a&quot;</code>", "<code>i = 1, v = &quot;a&quot;</code>", "<code>i = &quot;a&quot;, v = 0</code>"],
      answer: 1,
      why: "<code>enumerate</code> yields <code>(index, value)</code> pairs starting at 0, which saves you from the manual counter. Pass <code>start=1</code> if you want 1-based numbering.",
      wrong: { 0: "That would be iterating over pairs of values. <code>enumerate</code> always supplies the counter itself as the first item.",
               2: "It starts at 0 by default. <code>enumerate(seq, 1)</code> is how you get 1.",
               3: "The order is index first, value second." } },

    { tag: "References",
      q: "<code>a = [1, 2]</code>, then <code>b = a</code>, then <code>b.append(3)</code>. What is <code>a</code>?",
      choices: ["<code>[1, 2]</code>", "<code>[1, 2, 3]</code>", "<code>[3]</code>", "<code>None</code>"],
      answer: 1,
      why: "<code>b = a</code> copies the reference, not the list. Both names point at one object, so a mutation through either is visible through the other. Use <code>b = a[:]</code> or <code>list(a)</code> for a real copy.",
      wrong: { 0: "That is what you would get if assignment copied the data. It does not — it binds another name to the same object.",
               2: "<code>append</code> adds to what is already there; it does not replace the contents.",
               3: "<code>a</code> is still a perfectly good list. Only <code>append</code>'s return value is <code>None</code>." } },

    { tag: "Floats",
      q: "What does <code>0.1 + 0.2 == 0.3</code> evaluate to?",
      choices: ["<code>True</code>", "<code>False</code>", "<code>TypeError</code>", "It depends on the operating system"],
      answer: 1,
      why: "Binary floating point cannot represent 0.1 or 0.2 exactly, so the sum lands on 0.30000000000000004. Compare with <code>math.isclose()</code>, or use <code>decimal.Decimal</code> for money.",
      wrong: { 0: "It looks like it should be true, which is what makes this such a common bug. The stored values are tiny approximations.",
               2: "Adding two floats and comparing is perfectly valid — it just gives a surprising answer.",
               3: "This is IEEE 754 behavior, identical on every mainstream platform and in most other languages too." } },

    { tag: "Files",
      q: "What is the point of <code>with open(&quot;f.txt&quot;) as f:</code>?",
      choices: ["It opens the file faster", "It closes the file automatically, even if an exception is raised", "It loads the whole file into memory", "It locks the file so no one else can read it"],
      answer: 1,
      why: "<code>with</code> uses the context-manager protocol: the file's cleanup runs the moment the block exits, whether normally or via an exception. No leaked file handles.",
      wrong: { 0: "Opening speed is unchanged. The benefit is guaranteed cleanup.",
               2: "You still choose how to read — <code>.read()</code>, <code>.readlines()</code>, or line by line.",
               3: "<code>with</code> does no locking at all. That would need the <code>fcntl</code> or <code>msvcrt</code> module." } },

    { tag: "Errors",
      q: "Which block runs whether or not an exception was raised?",
      choices: ["<code>except</code>", "<code>else</code>", "<code>finally</code>", "<code>raise</code>"],
      answer: 2,
      why: "<code>finally</code> always executes on the way out of the <code>try</code> — after success, after a handled exception, even during an unhandled one. It is where cleanup belongs.",
      wrong: { 0: "<code>except</code> only runs when a matching exception was actually raised.",
               1: "<code>else</code> is the opposite — it runs only when <em>no</em> exception was raised.",
               3: "<code>raise</code> is a statement that throws an exception, not a block that runs on the way out." } },

    { tag: "Strings",
      q: "<code>s = &quot;hi&quot;</code> then <code>s.upper()</code>. What is <code>s</code> afterwards?",
      choices: ["<code>&quot;HI&quot;</code>", "<code>&quot;hi&quot;</code>", "<code>None</code>", "<code>[&quot;H&quot;, &quot;I&quot;]</code>"],
      answer: 1,
      why: "Strings are immutable, so every string method returns a <strong>new</strong> string and leaves the original alone. You have to write <code>s = s.upper()</code> to keep the result.",
      wrong: { 0: "That is the value <code>upper()</code> returned — but it was thrown away because it was never assigned.",
               2: "String methods return strings. <code>None</code> comes from in-place methods like <code>list.sort()</code>.",
               3: "<code>upper()</code> returns one string, not a list of characters. That would be <code>list(s.upper())</code>." } },

    { tag: "Truthiness",
      q: "Which value is <em>truthy</em> in Python?",
      choices: ["<code>[]</code>", "<code>0</code>", "<code>&quot;&quot;</code>", "<code>[0]</code>"],
      answer: 3,
      why: "Emptiness is what makes a container falsy, not its contents. <code>[0]</code> has one element, so it is truthy even though that element is itself falsy.",
      wrong: { 0: "An empty list is falsy — which is why <code>if not items:</code> is the idiomatic empty check.",
               1: "Zero is falsy for every numeric type: <code>0</code>, <code>0.0</code>, <code>0j</code>.",
               2: "The empty string is falsy. Any other string, including <code>&quot;0&quot;</code> and <code>&quot; &quot;</code>, is truthy." } },

    { tag: "Data model",
      q: "What does <code>list(range(2, 10, 3))</code> produce?",
      choices: ["<code>[2, 5, 8]</code>", "<code>[2, 5, 8, 11]</code>", "<code>[3, 6, 9]</code>", "<code>[2, 3, 10]</code>"],
      answer: 0,
      why: "The arguments are start, stop, step: begin at 2, add 3 each time, stop before 10. That gives 2, 5, 8 — 11 would overshoot the stop value anyway.",
      wrong: { 1: "11 is past the stop value of 10, and <code>range</code> never reaches or passes it.",
               2: "That is <code>range(3, 10, 3)</code>. The first argument is the starting value, not the step.",
               3: "These are the three arguments themselves, not the sequence they describe." } }
  ]
});
