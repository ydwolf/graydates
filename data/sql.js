GD.addTopic({
  id: "sql",
  name: "SQL & Databases",
  sigil: "SQL",
  blurb: "Joins, keys, indexes and transactions",
  questions: [
    { tag: "Queries",
      q: "Which query returns only users in Ohio?",
      choices: ["<code>SELECT * FROM users HAVING state = &#39;OH&#39;</code>", "<code>SELECT * FROM users WHERE state = &#39;OH&#39;</code>", "<code>SELECT users WHERE state = &#39;OH&#39;</code>", "<code>GET * FROM users WHERE state = &#39;OH&#39;</code>"],
      answer: 1,
      why: "<code>WHERE</code> filters individual rows before any grouping happens. The shape is SELECT columns FROM table WHERE condition.",
      wrong: { 0: "<code>HAVING</code> filters <em>groups</em> after <code>GROUP BY</code>. Using it here is either an error or a needless full scan.",
               2: "<code>FROM</code> is missing — <code>SELECT</code> needs to know which table to read.",
               3: "<code>GET</code> is an HTTP method. SQL reads with <code>SELECT</code>." } },

    { tag: "Keys",
      q: "What does a primary key guarantee?",
      choices: ["The column is sorted alphabetically", "Each row is uniquely identifiable, and the value is never NULL", "The column is encrypted", "The table can only have one row"],
      answer: 1,
      why: "A primary key is unique and NOT NULL, giving every row a stable identity. Databases index it automatically, so lookups by key are fast.",
      wrong: { 0: "The physical order is an implementation detail; use <code>ORDER BY</code> when you need sorted results.",
               2: "Keys are stored in plain form. Encryption is a separate concern.",
               3: "It constrains each row's identity, not how many rows there are." } },

    { tag: "Keys",
      q: "A foreign key does what?",
      choices: ["Points at a primary key in another table, enforcing that the reference exists", "Encrypts the column", "Makes the column optional", "Creates a copy of another table"],
      answer: 0,
      why: "It is referential integrity: an <code>orders.user_id</code> foreign key means the database will reject an order pointing at a user who does not exist, and can block deleting a user who still has orders.",
      wrong: { 1: "Nothing about a foreign key involves encryption.",
               2: "It can be nullable or not — that is a separate constraint entirely.",
               3: "No data is copied. Only a reference is stored and checked." } },

    { tag: "Joins",
      q: "What is the difference between INNER JOIN and LEFT JOIN?",
      choices: ["No difference", "INNER keeps only matching rows; LEFT keeps every left row, filling NULLs where there is no match", "LEFT is faster", "INNER only works on two tables"],
      answer: 1,
      why: "INNER is the intersection. LEFT preserves the left table in full — which is how you find users with <em>no</em> orders: LEFT JOIN, then <code>WHERE orders.id IS NULL</code>.",
      wrong: { 0: "The row counts differ whenever any left row lacks a match.",
               2: "Speed depends on indexes and data, and LEFT usually returns <em>more</em> rows.",
               3: "You can chain as many joins as you need with either kind." } },

    { tag: "Aggregates",
      q: "Which query counts orders per customer?",
      choices: ["<code>SELECT customer_id, COUNT(*) FROM orders</code>", "<code>SELECT customer_id, COUNT(*) FROM orders GROUP BY customer_id</code>", "<code>SELECT COUNT(customer_id) FROM orders</code>", "<code>SELECT customer_id FROM orders ORDER BY COUNT(*)</code>"],
      answer: 1,
      why: "<code>GROUP BY</code> collapses rows into one per customer, and the aggregate is computed within each group. Any non-aggregated column in the SELECT must appear in the GROUP BY.",
      wrong: { 0: "Without <code>GROUP BY</code>, mixing a plain column with an aggregate is an error in strict databases and nonsense elsewhere.",
               2: "That returns one number — the total count across all customers.",
               3: "<code>ORDER BY</code> only sorts. It does not group anything." } },

    { tag: "Aggregates",
      q: "When do you need HAVING instead of WHERE?",
      choices: ["When filtering on the result of an aggregate, such as <code>COUNT(*) &gt; 5</code>", "When the table is large", "When joining three tables", "HAVING is just an alias for WHERE"],
      answer: 0,
      why: "<code>WHERE</code> runs before grouping, so the aggregate does not exist yet. <code>HAVING</code> runs after, which is the only place you can test a group's computed value.",
      wrong: { 1: "Size is irrelevant to which clause is correct.",
               2: "Join conditions belong in <code>ON</code> or <code>WHERE</code>.",
               3: "They act at different stages of query execution and are not interchangeable." } },

    { tag: "NULL",
      q: "Why does <code>WHERE email = NULL</code> never match anything?",
      choices: ["NULL is not a valid keyword", "NULL means unknown, so any comparison to it yields unknown — use <code>IS NULL</code>", "It matches only empty strings", "It is valid but slow"],
      answer: 1,
      why: "SQL uses three-valued logic. <code>NULL = NULL</code> is not true but <em>unknown</em>, and <code>WHERE</code> keeps only rows that are true. The special operators <code>IS NULL</code> and <code>IS NOT NULL</code> exist for exactly this.",
      wrong: { 0: "<code>NULL</code> is a perfectly valid keyword — it just cannot be compared with <code>=</code>.",
               2: "An empty string is a known value and is distinct from NULL.",
               3: "It is not a matter of speed. It returns zero rows, always." } },

    { tag: "Aggregates",
      q: "A column has 10 rows, 3 of them NULL. What do <code>COUNT(*)</code> and <code>COUNT(col)</code> return?",
      choices: ["10 and 10", "10 and 7", "7 and 7", "7 and 10"],
      answer: 1,
      why: "<code>COUNT(*)</code> counts rows regardless of content. <code>COUNT(col)</code> counts non-NULL values in that column, so it skips the three.",
      wrong: { 0: "A column count never includes NULLs — that difference is often the bug.",
               2: "<code>COUNT(*)</code> does not inspect any column, so nothing is skipped.",
               3: "Exactly reversed." } },

    { tag: "Indexes",
      q: "What is the trade-off of adding an index?",
      choices: ["Faster reads on the indexed column, slower writes and more storage", "Faster everything", "It compresses the table", "It prevents duplicate rows"],
      answer: 0,
      why: "An index is a sorted structure the database maintains alongside the table. Lookups stop being full scans, but every INSERT, UPDATE and DELETE must also update the index.",
      wrong: { 1: "Writes get slower. That is why you index the columns you filter and join on, not every column.",
               2: "Indexes add storage rather than reducing it.",
               3: "Only a UNIQUE index does that, and uniqueness is the constraint, not the index itself." } },

    { tag: "Transactions",
      q: "What does ACID stand for?",
      choices: ["Access, Control, Integrity, Durability", "Atomicity, Consistency, Isolation, Durability", "Aggregate, Count, Index, Delete", "Async, Cached, Indexed, Distributed"],
      answer: 1,
      why: "Atomicity (all or nothing), Consistency (constraints hold), Isolation (concurrent transactions do not see each other's half-finished work), Durability (a commit survives a crash).",
      wrong: { 0: "Access control is a permissions concern, not part of the transaction guarantees.",
               2: "These are query operations, not properties of a transaction.",
               3: "These are distributed-systems adjectives, several of which ACID databases deliberately trade against." } },

    { tag: "Transactions",
      q: "A transfer debits one account and credits another, and the second statement fails. What should happen?",
      choices: ["The debit stands", "The whole transaction rolls back, leaving both balances untouched", "The database retries forever", "Only the credit is applied"],
      answer: 1,
      why: "Atomicity means a transaction is indivisible. <code>ROLLBACK</code> discards every change since <code>BEGIN</code>, so money cannot evaporate between accounts.",
      wrong: { 0: "A standing debit with no matching credit is exactly the corruption transactions exist to prevent.",
               2: "Retrying is a decision for your application, not automatic behavior.",
               3: "The credit is the statement that failed, and partial application is never correct here." } },

    { tag: "Design",
      q: "What is the point of normalization?",
      choices: ["Making all values the same length", "Removing redundant data so each fact is stored in exactly one place", "Encrypting sensitive columns", "Speeding up every query"],
      answer: 1,
      why: "If a customer's address lives in one row rather than being repeated on every order, it cannot become inconsistent and an update touches one place. Deliberate denormalization is a later performance trade.",
      wrong: { 0: "That is padding or a data-type choice, not normalization.",
               2: "Encryption is orthogonal to schema design.",
               3: "Normalization often <em>adds</em> joins, which can slow reads. The gain is integrity." } },

    { tag: "Design",
      q: "Students take many courses and courses have many students. How is that modeled?",
      choices: ["A comma-separated list of course IDs in the students table", "A join table with one row per (student, course) pair", "One table with 50 course columns", "A foreign key in each direction"],
      answer: 1,
      why: "A junction table — <code>enrollments(student_id, course_id)</code> — is the standard way to express many-to-many, and it gives you a natural home for extras like enrollment date or grade.",
      wrong: { 0: "Packing multiple values into one field breaks first normal form and makes joins and indexing impossible.",
               2: "A fixed set of columns caps the relationship arbitrarily and wastes space on NULLs.",
               3: "Foreign keys point one-to-many. Two of them cannot express many-to-many." } },

    { tag: "Security",
      q: "What is the correct defense against SQL injection?",
      choices: ["Stripping the word DROP from input", "Parameterized queries (prepared statements)", "Making the query shorter", "Using POST instead of GET"],
      answer: 1,
      why: "Parameters keep code and data on separate channels — the database receives the query structure first, so a value can never be reinterpreted as SQL no matter what it contains.",
      wrong: { 0: "Blacklists always leak. Attackers have endless encodings and phrasings.",
               2: "Length has nothing to do with it.",
               3: "The HTTP method changes where the data rides, not how it is interpolated into SQL." } },

    { tag: "Queries",
      q: "You run <code>UPDATE users SET active = 0</code> and forget the WHERE clause. What happens?",
      choices: ["Nothing — it is rejected", "Only the first row updates", "Every row in the table is updated", "It updates a random row"],
      answer: 2,
      why: "No WHERE means no filter, so the statement applies to the entire table. This is why you run the SELECT first and wrap destructive changes in a transaction you can roll back.",
      wrong: { 0: "It is valid SQL. Most databases run it without complaint.",
               1: "There is no implicit limit. That would require an explicit <code>LIMIT</code>.",
               3: "Nothing random about it — it hits all of them." } },

    { tag: "Queries",
      q: "What does <code>SELECT DISTINCT state FROM users</code> return?",
      choices: ["Every state value, with duplicates", "Each distinct state once", "The most common state", "The number of states"],
      answer: 1,
      why: "<code>DISTINCT</code> collapses duplicate result rows, giving one row per unique value.",
      wrong: { 0: "That is the query without <code>DISTINCT</code>.",
               2: "That would need <code>GROUP BY state ORDER BY COUNT(*) DESC LIMIT 1</code>.",
               3: "For a count you would write <code>COUNT(DISTINCT state)</code>." } },

    { tag: "Queries",
      q: "<code>ORDER BY created_at</code> with no direction sorts how?",
      choices: ["Ascending — oldest first", "Descending — newest first", "Randomly", "By primary key"],
      answer: 0,
      why: "<code>ASC</code> is the default. For newest-first you have to write <code>ORDER BY created_at DESC</code> explicitly.",
      wrong: { 1: "Descending requires the keyword. Forgetting it is a common bug in &quot;latest posts&quot; queries.",
               2: "Sorting is deterministic; randomness would need something like <code>ORDER BY RANDOM()</code>.",
               3: "Without an ORDER BY the order is unspecified — but here you named a column." } },

    { tag: "Commands",
      q: "How do DELETE, TRUNCATE and DROP differ?",
      choices: ["They are synonyms", "DELETE removes selected rows; TRUNCATE empties the table; DROP removes the table itself", "DELETE removes the table; DROP removes rows", "All three remove the whole database"],
      answer: 1,
      why: "DELETE is a row operation and can be filtered with WHERE and rolled back. TRUNCATE wipes all rows quickly, leaving the structure. DROP removes the table definition entirely.",
      wrong: { 0: "Confusing them is how people lose tables they meant to filter.",
               2: "Exactly backwards.",
               3: "Dropping a database takes <code>DROP DATABASE</code> — a separate and much scarier statement." } },

    { tag: "Paging",
      q: "What does <code>LIMIT 10 OFFSET 20</code> do?",
      choices: ["Returns rows 21 through 30", "Returns the first 10 rows", "Returns 20 rows", "Returns rows 10 through 20"],
      answer: 0,
      why: "OFFSET skips the first 20 rows and LIMIT takes the next 10 — page 3 at ten per page. Pair it with a stable <code>ORDER BY</code>, or paging results will shuffle between requests.",
      wrong: { 1: "That is <code>LIMIT 10</code> with no offset.",
               2: "The 20 is how many to skip, not how many to return.",
               3: "That would be <code>LIMIT 11 OFFSET 9</code>." } },

    { tag: "Concepts",
      q: "The main structural difference between a relational database and a document store like MongoDB is —",
      choices: ["Relational databases cannot scale", "Relational uses fixed-schema tables with joins; document stores keep flexible nested documents", "Document stores cannot store numbers", "There is no difference"],
      answer: 1,
      why: "Relational databases enforce a schema and assemble data through joins. Document stores keep related data nested in one document, trading join power and rigid structure for flexibility.",
      wrong: { 0: "Relational databases run enormous systems. Scaling writes horizontally is harder, but that is a trade-off, not a ceiling.",
               2: "Document stores handle numbers, dates, arrays and nested objects natively.",
               3: "The data models differ substantially, and so do the queries you write." } }
  ]
});
