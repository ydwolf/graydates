GD.addTopic({
  id: "web",
  name: "Web Programming",
  sigil: "</>",
  blurb: "HTML, CSS, JavaScript and the request cycle",
  questions: [
    { tag: "Fundamentals",
      q: "What is each of the three front-end languages responsible for?",
      choices: ["HTML styles, CSS structures, JS stores data", "HTML structures, CSS styles, JS adds behavior", "HTML runs on the server, CSS on the client, JS on both", "They are three syntaxes for the same thing"],
      answer: 1,
      why: "HTML marks up content and meaning, CSS controls presentation, JavaScript adds interaction. Keeping them separated is what lets you restyle a page without touching its content.",
      wrong: { 0: "Reversed on the first two, and JavaScript is a full programming language, not a store.",
               2: "All three are delivered to the browser and run on the client. JavaScript can <em>also</em> run server-side under Node.",
               3: "They are three different languages with different grammars and different jobs." } },

    { tag: "CSS",
      q: "Which selector wins when <code>#box</code>, <code>.card</code> and <code>div</code> all set a color on the same element?",
      choices: ["<code>div</code>", "<code>.card</code>", "<code>#box</code>", "Whichever is written last"],
      answer: 2,
      why: "Specificity is scored in order — inline style, then ID, then class/attribute/pseudo-class, then element. One ID outranks any number of classes, so <code>#box</code> wins regardless of source order.",
      wrong: { 0: "Element selectors are the weakest tier of all.",
               1: "A class beats an element but loses to an ID.",
               3: "Source order only breaks ties <em>within</em> the same specificity score." } },

    { tag: "CSS",
      q: "What does <code>box-sizing: border-box</code> change?",
      choices: ["Padding and border are included inside the declared width", "It removes all borders", "It turns the element into a flex container", "It centers the element"],
      answer: 0,
      why: "The default <code>content-box</code> adds padding and border <em>on top of</em> <code>width</code>, so a 200px box with 20px padding occupies 240px. <code>border-box</code> folds them inward — the element stays exactly 200px.",
      wrong: { 1: "The border still renders; it is only measured differently.",
               2: "That is <code>display: flex</code>, a separate property.",
               3: "Centering is <code>margin: auto</code> or a flex/grid alignment." } },

    { tag: "CSS",
      q: "In a default flex row, which property spaces items along the horizontal axis?",
      choices: ["<code>align-items</code>", "<code>justify-content</code>", "<code>text-align</code>", "<code>vertical-align</code>"],
      answer: 1,
      why: "<code>justify-content</code> works on the main axis — horizontal in a row — and gives you <code>space-between</code>, <code>center</code>, and friends. <code>align-items</code> handles the cross axis.",
      wrong: { 0: "<code>align-items</code> positions items vertically in a row, i.e. on the cross axis.",
               2: "<code>text-align</code> aligns inline text inside a box, not flex children.",
               3: "<code>vertical-align</code> applies to inline and table-cell layout and has no effect on flex items." } },

    { tag: "HTTP",
      q: "A request returns <code>500</code>. What does that tell you?",
      choices: ["The page was not found", "The server hit an error handling the request", "The resource moved permanently", "The client is not authenticated"],
      answer: 1,
      why: "5xx codes are server-side failures — the request was understood but the server broke while handling it. Read the server logs, not the browser console.",
      wrong: { 0: "Not found is <code>404</code>, a 4xx client-side code.",
               2: "Moved permanently is <code>301</code>, a redirect.",
               3: "That is <code>401</code> (unauthenticated) or <code>403</code> (forbidden)." } },

    { tag: "HTTP",
      q: "What is the practical difference between GET and POST?",
      choices: ["GET is faster", "GET requests data with parameters in the URL; POST sends data in the request body to change something", "POST only works over HTTPS", "GET can send files, POST cannot"],
      answer: 1,
      why: "GET is meant to be safe and repeatable — it can be cached, bookmarked and re-sent. POST carries a body and is expected to have side effects, which is why browsers warn before re-submitting one.",
      wrong: { 0: "Speed is essentially identical. The difference is semantics and where the data rides.",
               2: "Both work over plain HTTP; both should use HTTPS. The protocol is independent of the method.",
               3: "Backwards — file uploads use POST with a multipart body. URLs are far too small for files." } },

    { tag: "DOM",
      q: "What does <code>document.querySelector(&quot;.item&quot;)</code> return?",
      choices: ["All elements with class <code>item</code>", "The first element matching <code>.item</code>, or <code>null</code>", "An array of elements", "The text inside the element"],
      answer: 1,
      why: "<code>querySelector</code> takes any CSS selector and returns the first match in document order, or <code>null</code> if nothing matches. <code>querySelectorAll</code> is what returns every match.",
      wrong: { 0: "That is <code>querySelectorAll</code>, which gives a NodeList.",
               2: "Even <code>querySelectorAll</code> returns a NodeList, not a true array — you often need <code>Array.from()</code> on it.",
               3: "You get the element object. Its text is <code>.textContent</code>." } },

    { tag: "Events",
      q: "A click on a button inside a div fires the div's click handler too. What is that called?",
      choices: ["Event bubbling", "Event throttling", "Hoisting", "Reflow"],
      answer: 0,
      why: "After firing on the target, an event propagates up through its ancestors — that is bubbling. It enables event delegation, where one listener on a parent handles clicks for hundreds of children.",
      wrong: { 1: "Throttling limits how often a handler may run, for events like scroll.",
               2: "Hoisting is how JavaScript moves declarations to the top of their scope.",
               3: "Reflow is the browser recalculating layout — a rendering cost, not a propagation model." } },

    { tag: "JavaScript",
      q: "What is the key difference between <code>let</code> and <code>var</code>?",
      choices: ["<code>let</code> is block-scoped; <code>var</code> is function-scoped", "<code>let</code> cannot be reassigned", "<code>var</code> is newer", "They are identical"],
      answer: 0,
      why: "A <code>var</code> declared inside an <code>if</code> block leaks to the whole enclosing function; a <code>let</code> stops at the closing brace. Block scoping is why <code>let</code> in a loop captures the value you expect.",
      wrong: { 1: "That is <code>const</code>. <code>let</code> can be reassigned freely.",
               2: "Backwards — <code>var</code> is the original; <code>let</code> and <code>const</code> arrived with ES6 in 2015.",
               3: "They differ in scope, in hoisting behavior, and in whether redeclaration is allowed." } },

    { tag: "JavaScript",
      q: "What does <code>&quot;5&quot; == 5</code> evaluate to, and <code>&quot;5&quot; === 5</code>?",
      choices: ["<code>true</code> and <code>true</code>", "<code>true</code> and <code>false</code>", "<code>false</code> and <code>false</code>", "<code>false</code> and <code>true</code>"],
      answer: 1,
      why: "<code>==</code> coerces types before comparing, so the string converts to a number and matches. <code>===</code> compares type as well as value, and a string is never a number. Prefer <code>===</code>.",
      wrong: { 0: "<code>===</code> is strict — different types mean <code>false</code>, full stop.",
               2: "Loose <code>==</code> really does coerce here and return <code>true</code>. That surprise is exactly why it is discouraged.",
               3: "Exactly inverted." } },

    { tag: "Async",
      q: "What does <code>fetch(&quot;/api&quot;)</code> return?",
      choices: ["The response body as a string", "A Promise that resolves to a Response object", "Parsed JSON", "<code>undefined</code> until the request finishes"],
      answer: 1,
      why: "<code>fetch</code> returns a Promise resolving to a Response. Getting the data takes a second step — <code>await res.json()</code> — because the body arrives as a stream.",
      wrong: { 0: "Nothing is available synchronously; the network has not answered yet.",
               2: "Parsing is a separate call on the Response, and it is also asynchronous.",
               3: "You get a real Promise object immediately, which is how you attach <code>.then</code> or <code>await</code> it." } },

    { tag: "Storage",
      q: "How do <code>localStorage</code> and <code>sessionStorage</code> differ?",
      choices: ["<code>localStorage</code> persists after the tab closes; <code>sessionStorage</code> is cleared with the tab", "<code>localStorage</code> is server-side", "<code>sessionStorage</code> holds more data", "They are the same API with different names"],
      answer: 0,
      why: "Both store strings per origin, but <code>sessionStorage</code> dies with the tab while <code>localStorage</code> survives restarts until code or the user clears it. That is what lets this quiz remember your progress.",
      wrong: { 1: "Both live entirely in the browser. The server never sees them unless you send the values yourself.",
               2: "The quota is comparable, typically around 5 MB for each.",
               3: "Same API surface, genuinely different lifetimes." } },

    { tag: "JSON",
      q: "Which converts a JavaScript object into a string for storage or transmission?",
      choices: ["<code>JSON.parse(obj)</code>", "<code>JSON.stringify(obj)</code>", "<code>obj.toJSON()</code>", "<code>String(obj)</code>"],
      answer: 1,
      why: "<code>JSON.stringify</code> serializes an object to JSON text; <code>JSON.parse</code> goes the other way. Remember it as stringify = out to text, parse = back to objects.",
      wrong: { 0: "<code>parse</code> takes a string and gives back an object — the opposite direction.",
               2: "<code>toJSON</code> is an optional hook that <em>customizes</em> serialization; it is not the entry point and most objects lack it.",
               3: "<code>String(obj)</code> yields the useless <code>&quot;[object Object]&quot;</code>." } },

    { tag: "CSS",
      q: "How do <code>display: none</code> and <code>visibility: hidden</code> differ?",
      choices: ["No difference", "<code>display: none</code> removes the element from layout; <code>visibility: hidden</code> hides it but keeps its space", "<code>visibility: hidden</code> deletes the element from the DOM", "<code>display: none</code> only works on images"],
      answer: 1,
      why: "<code>display: none</code> takes the element out of the flow entirely — the gap closes. <code>visibility: hidden</code> leaves an invisible hole of exactly the same size.",
      wrong: { 0: "The layout consequence is completely different, and it is a common source of shifting UI.",
               2: "Neither removes nodes from the DOM. That takes <code>element.remove()</code>.",
               3: "It applies to every element." } },

    { tag: "Responsive",
      q: "What does <code>@media (max-width: 640px) { … }</code> do?",
      choices: ["Caps the page at 640px wide", "Applies the rules inside only when the viewport is 640px or narrower", "Loads a separate mobile site", "Sets every element to 640px"],
      answer: 1,
      why: "A media query gates a block of CSS on a condition about the device or viewport. That is how one stylesheet adapts a layout to phones, tablets and desktops.",
      wrong: { 0: "Capping width is <code>max-width</code> on an element, not a media query.",
               2: "No navigation happens. It is the same page with different rules applied.",
               3: "It sets nothing on its own — it only decides whether its contents apply." } },

    { tag: "Async",
      q: "What does this print, in order?<pre>console.log(&quot;A&quot;);\nsetTimeout(() =&gt; console.log(&quot;B&quot;), 0);\nconsole.log(&quot;C&quot;);</pre>",
      choices: ["A B C", "A C B", "B A C", "C A B"],
      answer: 1,
      why: "Even with a 0 ms delay, the callback is queued and can only run once the call stack is empty. All synchronous code finishes first — so A, C, then B.",
      wrong: { 0: "That would require <code>setTimeout</code> to run immediately and synchronously, which it never does.",
               2: "A timeout can never jump ahead of code already running.",
               3: "<code>console.log(&quot;A&quot;)</code> runs first; it is the first statement." } },

    { tag: "Security",
      q: "What is CORS?",
      choices: ["A CSS layout system", "A browser rule requiring a server to opt in before another origin's JavaScript may read its response", "A compression algorithm", "A JavaScript framework"],
      answer: 1,
      why: "Cross-Origin Resource Sharing relaxes the same-origin policy. The browser blocks the reading of a cross-origin response unless the server returns headers such as <code>Access-Control-Allow-Origin</code> — which is why CORS errors are fixed on the server.",
      wrong: { 0: "Layout systems are Flexbox and Grid.",
               2: "Compression on the wire is gzip or Brotli.",
               3: "It is a browser security mechanism, not a library you install." } },

    { tag: "HTML",
      q: "Why use <code>&lt;nav&gt;</code>, <code>&lt;main&gt;</code> and <code>&lt;article&gt;</code> instead of <code>&lt;div&gt;</code> everywhere?",
      choices: ["They render faster", "They convey meaning, which screen readers and search engines can use", "They are required by HTML5", "They style themselves automatically"],
      answer: 1,
      why: "Semantic elements describe the role of a region. Assistive technology can jump straight to <code>&lt;main&gt;</code> or list the page's landmarks, and crawlers read structure more reliably.",
      wrong: { 0: "Rendering cost is effectively identical to a <code>div</code>.",
               2: "A page of nothing but divs is still valid HTML5. Semantics are a quality choice, not a requirement.",
               3: "They carry almost no default styling beyond block display." } },

    { tag: "Security",
      q: "What does HTTPS give you that HTTP does not?",
      choices: ["Faster page loads", "Encryption in transit plus verification of the server's identity", "Protection from all bugs in your code", "Automatic backups"],
      answer: 1,
      why: "TLS encrypts the traffic so intermediaries cannot read or alter it, and the certificate proves you are talking to the domain you think you are. It says nothing about the quality of the site behind it.",
      wrong: { 0: "Modern HTTPS with HTTP/2 is often faster, but that is a side effect of the protocol version, not of encryption itself.",
               2: "An XSS hole or SQL injection is just as exploitable over HTTPS.",
               3: "Backups are infrastructure and have nothing to do with the transport." } },

    { tag: "DOM",
      q: "What is the DOM?",
      choices: ["A tree of objects representing the page, which JavaScript can read and modify", "The HTML file on disk", "A CSS preprocessor", "The browser's network cache"],
      answer: 0,
      why: "The browser parses HTML into a live tree of node objects. Your JavaScript manipulates that tree, and the rendering engine redraws — which is why DOM changes appear without reloading the file.",
      wrong: { 1: "The file is the source text. The DOM is the in-memory structure built from it, and it keeps changing after load.",
               2: "Preprocessors like Sass compile to CSS before the browser ever sees it.",
               3: "The cache stores fetched resources; it is unrelated to page structure." } }
  ]
});
